'use strict';

Liferay.Loader.define('my-react-app$fs-extra@8.1.0/lib/index', ['module', 'exports', 'require', './fs', './copy-sync', './copy', './empty', './ensure', './json', './mkdirs', './move-sync', './move', './output', './path-exists', './remove', 'my-react-app$fs'], function (module, exports, require) {
  var define = undefined;
  var global = window;
  {
    module.exports = Object.assign({},
    // Export promiseified graceful-fs:
    require('./fs'),
    // Export extra methods:
    require('./copy-sync'), require('./copy'), require('./empty'), require('./ensure'), require('./json'), require('./mkdirs'), require('./move-sync'), require('./move'), require('./output'), require('./path-exists'), require('./remove'));

    // Export fs.promises as a getter property so that we don't trigger
    // ExperimentalWarning before fs.promises is actually accessed.
    const fs = require('my-react-app$fs');
    if (Object.getOwnPropertyDescriptor(fs, 'promises')) {
      Object.defineProperty(module.exports, 'promises', {
        get() {
          return fs.promises;
        }
      });
    }
  }
});
//# sourceMappingURL=index.js.map