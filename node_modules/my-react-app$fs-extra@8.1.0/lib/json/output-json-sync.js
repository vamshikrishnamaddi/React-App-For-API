'use strict';

Liferay.Loader.define('my-react-app$fs-extra@8.1.0/lib/json/output-json-sync', ['module', 'exports', 'require', 'my-react-app$graceful-fs', 'my-react-app$path', '../mkdirs', './jsonfile'], function (module, exports, require) {
  var define = undefined;
  var global = window;
  {
    const fs = require('my-react-app$graceful-fs');
    const path = require('my-react-app$path');
    const mkdir = require('../mkdirs');
    const jsonFile = require('./jsonfile');

    function outputJsonSync(file, data, options) {
      const dir = path.dirname(file);

      if (!fs.existsSync(dir)) {
        mkdir.mkdirsSync(dir);
      }

      jsonFile.writeJsonSync(file, data, options);
    }

    module.exports = outputJsonSync;
  }
});
//# sourceMappingURL=output-json-sync.js.map