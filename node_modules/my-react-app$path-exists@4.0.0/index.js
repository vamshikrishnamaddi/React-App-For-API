'use strict';

Liferay.Loader.define('my-react-app$path-exists@4.0.0/index', ['module', 'exports', 'require', 'my-react-app$fs', 'my-react-app$util'], function (module, exports, require) {
	var define = undefined;
	var global = window;
	{
		const fs = require('my-react-app$fs');
		const { promisify } = require('my-react-app$util');

		const pAccess = promisify(fs.access);

		module.exports = async path => {
			try {
				await pAccess(path);
				return true;
			} catch (_) {
				return false;
			}
		};

		module.exports.sync = path => {
			try {
				fs.accessSync(path);
				return true;
			} catch (_) {
				return false;
			}
		};
	}
});
//# sourceMappingURL=index.js.map