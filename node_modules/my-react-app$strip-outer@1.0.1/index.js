'use strict';

Liferay.Loader.define('my-react-app$strip-outer@1.0.1/index', ['module', 'exports', 'require', 'my-react-app$escape-string-regexp'], function (module, exports, require) {
	var define = undefined;
	var global = window;
	{
		var escapeStringRegexp = require('my-react-app$escape-string-regexp');

		module.exports = function (str, sub) {
			if (typeof str !== 'string' || typeof sub !== 'string') {
				throw new TypeError();
			}

			sub = escapeStringRegexp(sub);
			return str.replace(new RegExp('^' + sub + '|' + sub + '$', 'g'), '');
		};
	}
});
//# sourceMappingURL=index.js.map