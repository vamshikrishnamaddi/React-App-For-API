'use strict';

Liferay.Loader.define('my-react-app$filenamify@4.3.0/filenamify-path', ['module', 'exports', 'require', 'my-react-app$path', './filenamify'], function (module, exports, require) {
	var define = undefined;
	var global = window;
	{
		const path = require('my-react-app$path');
		const filenamify = require('./filenamify');

		const filenamifyPath = (filePath, options) => {
			filePath = path.resolve(filePath);
			return path.join(path.dirname(filePath), filenamify(path.basename(filePath), options));
		};

		module.exports = filenamifyPath;
	}
});
//# sourceMappingURL=filenamify-path.js.map